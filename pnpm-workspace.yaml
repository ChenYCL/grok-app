# Single-package app workspace (required by pnpm when this file exists).
packages:
  - "."

# Allowlist for packages that may run install scripts (pnpm 10+).
onlyBuiltDependencies:
  - esbuild
  - canvas
  - "@swc/core"
