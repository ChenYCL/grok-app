import { IconGrokMark } from "@/components/icons";

/** App brand mark (Tabler Sparkles). */
export function GrokLogo({ size = 22 }: { size?: number }) {
  return <IconGrokMark size={size} className="grok-logo" />;
}
